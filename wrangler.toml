# Cloudflare Pages configuration
name = "school-management"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"

# Pages Functions are in /functions directory (auto-detected)
pages_build_output_dir = "dist"
